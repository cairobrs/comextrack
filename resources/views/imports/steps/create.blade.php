<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Nova Etapa do Processo') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            @if (session('status'))
                <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
                    <span class="block sm:inline">{{ session('status') }}</span>
                </div>
            @endif

            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900">
                    <h3 class="text-lg font-semibold mb-4">Nova etapa para o processo {{ $import->numero_processo }}</h3>

                    <form method="POST" action="{{ route('imports.steps.store', $import) }}">
                        @csrf

                        @include('imports.steps._form')

                        <div class="flex items-center justify-end mt-4">
                            <a href="{{ route('imports.show', $import) }}" class="text-gray-700 hover:text-gray-900 mr-4">Voltar para o processo</a>
                            <x-primary-button>
                                {{ __('Salvar') }}
                            </x-primary-button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>

